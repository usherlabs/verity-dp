[package]
name = "zkvm_guest"
version = "0.1.0"
edition = "2021"

[workspace]

[dependencies]
bincode = "1.3.3"
risc0-zkvm = { version = "1.1.2", default-features = false, features = ['std'] }
serde_json = "1.0"
verity-verify-private-transcript = { path = "../../../../../rs/verify-private-transcript" }
