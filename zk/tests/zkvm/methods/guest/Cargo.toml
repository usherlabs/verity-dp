[package]
name = "zkvm_guest"
version = "0.1.0"
edition = "2021"

[workspace]

[dependencies]
risc0-zkvm = { version = "1.1.2", default-features = false, features = ['std'] }
verity-verifier = { path = "../../../../../../verity/verifier", default-features = false, features = [
  "no-session",
] }
